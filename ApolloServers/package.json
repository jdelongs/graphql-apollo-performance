{
  "name": "ApolloServer",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run-server": "nodemon server.js --ignore db.json",
    "json-server": "json-server db.json --delay 500 --port 5000",
    "start": "npm-run-all --parallel json-server run-server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^3.11.1",
    "graphql": "^16.6.0",
    "nodemon": "^2.0.20",
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "json-server": "^0.17.1"
  }
}
